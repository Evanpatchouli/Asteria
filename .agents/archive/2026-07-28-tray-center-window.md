# 托盘窗口居中操作

状态：实现完成，等待用户人工验收

## 完成内容

- 系统托盘菜单新增“窗口居中”
- 窗口移动到主显示器工作区正中心
- 隐藏窗口居中时使用 `showInactive()` 显示，不抢占焦点
- 居中坐标立即写入现有窗口状态
- 保留当前鼠标穿透状态
- 默认右下角和已有位置恢复规则保持不变
- 中心坐标计算处理窗口大于工作区的边界情况
- 同步功能记录、本地开发说明和阶段交接

## 静态工程检查

- `pnpm format:check`：通过
- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm build`：通过
- `git diff --check`：通过
- UTF-8 BOM 检查：通过

## 测试说明

- 用户明确负责功能测试。
- Codex 未运行启动冒烟或功能测试。
- 交付时提供包含预期结果的人工测试清单。
