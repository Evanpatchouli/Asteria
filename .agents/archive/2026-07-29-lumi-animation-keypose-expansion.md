# Lumi 动画第二轮验收修复

状态：实现完成，等待用户人工验收

## 已完成

- 空闲状态固定身体像素，后侧尾巴以 `-28° / 0° / +28°` 三个姿势构建。
- 空闲循环静止 `2.15s`，随后在 `0.35s` 内快速往返摇尾三次。
- 思考状态按高透明度最大主体连通区域的中央支撑带计算语义根部。
- 独立校验器不复用生产锚点算法，测得思考根部漂移小于 `1px`。
- 成功动画新增七张跑步、压缩蹬地、回旋朝向和触地冲击姿势。
- 成功动画扩展为 `10 FPS`、24 个时间槽，总时长保持 `2.4s`。
- 保持助跑、回旋、落地和 `yeah!` 的 `0.8 / 0.8 / 0.4 / 0.4s` 分配。
- 同步资源格式、动画系统、修复方案、决策、经验和交接文档。

## 静态验证

- Lumi 资源重建与独立校验通过。
- 空闲三帧尾巴不同，稳定身体区域逐字节一致，锚点完全相同。
- 思考投影根部横向漂移为 `0px`，纵向漂移约 `0.000064px`。
- 成功图集包含 24 个时间槽和至少 13 个不同图像或锚点组合。
- `pnpm typecheck` 通过。
- `pnpm lint` 通过。
- `pnpm format:check` 通过。
- `pnpm build` 通过。
- `git diff --check` 通过。
- 本次触碰文本文件 UTF-8 BOM 检查通过。

未运行单元测试、应用、冒烟测试或功能测试；按用户要求由用户执行人工验收。
