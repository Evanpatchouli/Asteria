# MVP Phase 1：周期 5

状态：实现完成，等待用户人工验收

完成内容：

- 首次启动默认定位到主显示器工作区右下角，右侧和底部各保留 `24px`
- 横纵各至少 `64px` 可见的部分离屏位置精确恢复，不再强制整窗可见
- 可见范围不足时仅修正到最低可见范围，完全离屏时回退主屏右下角
- 显示器新增、移除或工作区变化时按相同规则恢复窗口
- 使用 CSS `app-region: drag` 提供原生无框窗口拖动
- 实现整窗口鼠标穿透
- 实现系统托盘显示、隐藏、鼠标穿透和退出入口
- 窗口重新显示时不抢占当前应用焦点
- 实现 Electron 单实例锁和第二实例唤醒
- 使用版本化 JSON 保存窗口位置与穿透状态
- 使用串行写入和临时文件原子替换保护窗口状态
- 同步交互、进程模型、配置、本地开发和 MVP 任务文档

不包含：

- PixiJS
- Pet Runtime 接线
- Claude Code Hook
- 智能透明区域命中测试
- 设置页面和托盘设置入口
- 安装包与自动更新

静态工程检查：

- `pnpm format:check`：通过
- `pnpm lint`：通过
- `pnpm typecheck`：通过
- `pnpm build`：通过
- `git diff --check`：通过

测试说明：

- 用户明确负责功能测试。
- Codex 未新增或运行功能测试、自动化测试或启动冒烟。
- 交付时提供包含预期结果的人工测试清单。
